# README

Copy the following file to their corresponding path in `/etc`. The same
directory structure has been followed in this `etc` folder (that is,
if you do a pwd here, keep the `/etc/...` part and you'll have the 
corresponding system path).
